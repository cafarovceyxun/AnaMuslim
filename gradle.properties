## For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
#
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx1024m -XX:MaxPermSize=256m
# org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
#
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
#Sat Jul 31 13:09:42 IST 2021
# -Xmx5g is the ceiling, not the resting size. G1 does not hand heap back on its own,
# so an idle daemon kept sitting on several GB between builds; the periodic GC lets it
# uncommit once a minute of quiet has passed.
org.gradle.jvmargs=-Xmx5g -XX:MaxMetaspaceSize=1g -Dfile.encoding\=UTF-8 -XX:G1PeriodicGCInterval=60000
# Kotlin compile daemon (JVM/Android sources)
kotlin.daemon.jvmargs=-Xmx3g -XX:G1PeriodicGCInterval=60000
# Kotlin/Native compiler runs in its own JVM; its default -Xmx3G is not enough to
# link the Compose Multiplatform framework, so raise it explicitly.
kotlin.native.jvmArgs=-Xmx4g
android.useAndroidX=true
android.enableJetifier=true
android.nonTransitiveRClass=false
android.nonFinalResIds=false
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=false
android.builtInKotlin=false
android.newDsl=false

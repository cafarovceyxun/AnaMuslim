<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Kept outside the synchronized `PrayerWidget/` folder on purpose: a file system synchronized
	     group adds everything it finds to the target's resources, and the target's own Info.plist
	     must not be copied as one. The app target's Info.plist sits at this level for the same
	     reason. -->
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>
